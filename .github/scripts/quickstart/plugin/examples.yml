- name: Example
  uses: siosios/metrics@master
  with:
    filename: metrics.plugin.<%= name %>.svg
    token: ${{ secrets.metrics_TOKEN }}
    base: ""
    plugin_<%= name %>: yes
  prod:
    skip: true
