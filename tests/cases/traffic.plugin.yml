- name: 🧮 Repositories traffic - Repositories traffic
  uses: siosios/metrics@latest
  with:
    token: MOCKED_TOKEN
    base: repositories
    plugin_traffic: 'yes'
    use_mocked_data: 'yes'
    verify: 'yes'
