- name: 📘 Repository template - Example
  uses: siosios/metrics@main
  with:
    template: repository
    token: MOCKED_TOKEN
    user: siosios
    repo: metrics
    plugin_lines: yes
    plugin_followup: yes
    plugin_projects: yes
    plugin_projects_repositories: siosios/metrics/projects/1
    use_mocked_data: yes
    verify: yes
